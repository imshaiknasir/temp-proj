{
  "name": "mytest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "prestart": "rm -rf allure-report allure-results playwright-report test-results",
    "test": "npx playwright test || true",
    "posttest": "npm run allure:generate && npm run allure:open",
    "allure:generate": "node -e \"require('node:fs').rmSync('./allure-report', { recursive: true, force: true })\" && node ./utils/allure-patcher.js && npx allure generate ./allure-results -o ./allure-report && node ./utils/allure-patcher.js",
    "allure:open": "npx allure open ./allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "@types/node": "^25.5.2",
    "allure": "^3.3.1",
    "allure-playwright": "^3.6.0"
  }
}
